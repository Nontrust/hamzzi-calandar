## 1. 용어집 데이터 구조 및 정책 정의

- [x] 1.1 `hamjji-brand-lexicon` 데이터 스키마(`id`, `defaultLabel`, `kitschLabel`, `description`, `usageScope`, `safetyLevel`)를 설계하고 저장 포맷을 결정한다 (검증: 샘플 용어 10개를 스키마 검증에 통과시킨다).
- [x] 1.2 금지어/주의어/허용 톤 규칙을 정의하고 카피 검수 규칙 문서에 반영한다 (검증: 금지어 포함 입력 시 차단 규칙이 동작한다).
- [x] 1.3 키치 라벨 미존재 시 기본 용어 fallback 정책을 구현 명세로 고정한다 (검증: 키치 라벨 누락 케이스에서 기본 라벨이 반환된다).

## 2. 역할/세션/보상 화면 용어 적용

- [x] 2.1 역할 선택 화면(`user-role-selection`)에 브랜드 라벨 매핑을 적용한다 (검증: `user A/B` 노출명이 햄찌 톤으로 표시된다).
- [x] 2.2 역할 라벨 보조 설명(권한/행동 범위)을 함께 노출하도록 UI 규칙을 반영한다 (검증: 라벨 의미가 텍스트로 확인 가능하다).
- [x] 2.3 세션/TODO/달성률 상태 라벨(`study-execution`)을 용어집 기반으로 치환한다 (검증: 진행/완료/이월 상태가 키치 문구로 표시된다).
- [x] 2.4 보상 카드(`memory-reward-system`)의 카드명은 `추억카` 톤으로 적용하고 잠금/해금은 기본 용어로 유지한다 (검증: 보상 보관함에서 카드명/상태/조건 문구가 함께 노출된다).

## 3. 연동/리포트 문구 적용

- [x] 3.1 캘린더 이벤트 제목 템플릿(`exam-schedule-integration`)을 브랜드 템플릿으로 교체한다 (검증: 접수/시험/발표 이벤트 제목이 새 템플릿으로 생성된다).
- [x] 3.2 캘린더/API 실패 메시지에 "브랜드 톤 + 재시도 안내" 이중 표기를 적용한다 (검증: 실패 메시지에서 원인/다음 행동이 동시에 보인다).
- [x] 3.3 면접 모드 라벨 및 리포트 헤더(`ai-interview-coaching`)를 햄찌 톤으로 치환한다 (검증: 리포트 상단 헤더가 용어집 매핑 기준으로 출력된다).
- [x] 3.4 면접 피드백 문구에 톤 안전 규칙을 적용한다 (검증: 비하/저속 표현 포함 시 문구 생성이 거부되거나 대체된다).

## 4. QA 및 운영 반영

- [x] 4.1 용어집 기준 화면 카피 QA 체크리스트를 작성한다 (검증: 역할/세션/보상/일정/면접 5개 영역 점검 항목이 모두 포함된다).
- [x] 4.2 캘린더 제목 길이/가독성 가이드와 fallback 규칙을 운영 문서에 반영한다 (검증: 운영 문서만으로 제목 축약/대체 규칙을 재현할 수 있다).
- [x] 4.3 회귀 검증을 수행해 기능 의미가 손상되지 않았는지 확인한다 (검증: 기존 핵심 기능 흐름 테스트가 모두 통과한다).

## 5. 기념일 계산기/달력 표기 추가

- [ ] 5.1 기념일 계산 규칙(기준일, D+N, 월/년 반복, 윤년/말일 보정)을 명세로 고정한다 (검증: 100일/1주년/말일 케이스 계산 결과가 일관된다).
- [x] 5.2 기념일 계산 결과를 달력 데이터 모델에 반영한다 (검증: 달력 월 뷰 데이터에 기념일 항목이 포함된다).
- [x] 5.3 달력에서 기념일을 일반 일정과 구분되는 뱃지/점 표시로 노출한다 (검증: 동일 날짜에 일반 일정과 기념일이 함께 있어도 구분 가능하다).
- [x] 5.4 기념일 제목 포맷(`기념일명 · D+N`)과 툴팁/보조 문구를 반영한다 (검증: 오늘 기준 남은/지난 일수 의미가 명확히 보인다).
- [ ] 5.5 회귀 검증 및 QA 체크리스트에 기념일 시나리오를 추가한다 (검증: 기념일 기능 추가 후 기존 일정/보상/면접 흐름이 유지된다).
