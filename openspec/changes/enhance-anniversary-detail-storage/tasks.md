## 1. 데이터 모델 / 마이그레이션

- [x] 1.1 기념일 상세 필드(`category`, `note`, `reminder_enabled`, `reminder_offset_days`, `rule_type`, `rule_value`)를 저장 구조에 추가한다.
- [x] 1.2 기본 삭제 잠금 컬럼과 기본 데이터 백필 마이그레이션을 반영한다.
- [x] 1.3 `study` 카테고리를 제거하고 `relationship` 카테고리를 추가한다.

## 2. 서버 저장소 / 핸들러

- [x] 2.1 `anniversaryStore` CRUD와 month-view 응답을 상세 필드까지 처리하도록 확장한다.
- [x] 2.2 `birthday`, `relationship`를 관리형 카테고리로 잠그고 수동 생성/수정/삭제를 차단한다.
- [x] 2.3 데모 사용자(`user-a`, `user-b`)의 공유 기념일을 함께 조회하도록 규칙을 반영한다.
- [x] 2.4 `relationship`의 100일/주년 마일스톤을 month-view에 포함한다.

## 3. 모바일 클라이언트 / 화면

- [x] 3.1 `anniversaryClient` 요청/응답 타입과 로컬 fallback 포맷을 상세 필드 구조로 확장한다.
- [x] 3.2 기념일 생성/수정 UI에 입력 검증과 날짜 포맷 유틸을 반영한다.
- [x] 3.3 생일/사귄날은 수정/삭제 버튼을 숨기고, 카테고리 선택지는 `anniversary`, `other`만 노출한다.
- [x] 3.4 홈/월간 일정에서 공유 기념일과 관계 마일스톤이 표시되도록 조정한다.

## 4. 검증

- [x] 4.1 서버 테스트로 상세 필드, 잠금 정책, 공유 조회, 관계 마일스톤을 검증한다.
- [x] 4.2 서버 실패 시 로컬 fallback으로 월간 일정/기념일 조회가 유지되는지 확인한다.
- [x] 4.3 QA 문서와 README를 현재 정책 기준으로 갱신한다.
