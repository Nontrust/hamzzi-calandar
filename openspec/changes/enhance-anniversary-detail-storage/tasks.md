## 1. 데이터 모델/마이그레이션 확장

- [x] 1.1 기념일 테이블(또는 저장 구조)에 상세 필드(`category`, `note`, `reminder_enabled`, `reminder_offset_days`, `rule_type`, `rule_value`)를 추가한다.
- [x] 1.2 기존 레코드에 대한 기본값 백필(backfill) 및 하위 호환 마이그레이션을 작성한다.
- [x] 1.3 마이그레이션 적용 후 스키마/기본값 검증 쿼리를 수행한다.

## 2. 서버 저장소/핸들러 반영

- [x] 2.1 `anniversaryStore`의 타입과 CRUD 로직을 상세 필드까지 처리하도록 확장한다.
- [x] 2.2 `handlers` 요청 검증에 상세 필드 유효성(rule 조합, 메모 길이, 오프셋 범위)을 추가한다.
- [x] 2.3 캘린더 조회 응답(`month-view`)에 기념일 상세 메타데이터를 포함하도록 서버 응답을 확장한다.

## 3. 모바일 클라이언트/화면 반영

- [x] 3.1 `anniversaryClient` 요청/응답 타입 및 로컬 폴백 저장 포맷을 상세 필드 구조로 확장한다.
- [x] 3.2 기념일 생성/수정 UI에 상세 입력 필드를 추가하고 입력값 사전 검증을 반영한다.
- [x] 3.3 메인/기념일 화면에서 상세 필드(카테고리/알림/메모 요약) 노출 규칙을 반영한다.

## 4. 안정성 검증

- [x] 4.1 외부 공휴일 API 실패 상황에서도 내부 기념일 상세 데이터 조회가 유지되는지 검증한다.
- [x] 4.2 서버/모바일 타입체크 및 테스트를 수행하고 회귀 이슈를 정리한다.
- [x] 4.3 수동 QA 체크리스트를 문서화한다(생성/수정/조회/기존 데이터 호환 케이스 포함).
