## 1. 데이터 모델 및 마이그레이션

- [x] 1.1 `anniversaries` 테이블(`id`, `user_id`, `name`, `base_date`, `rule_type`, `rule_value`, `is_active`, `created_at`)을 추가한다 (검증: 마이그레이션 실행 후 스키마가 생성된다).
- [x] 1.2 `user_id + base_date` 인덱스와 소유권 기반 RLS 정책을 추가한다 (검증: 본인 데이터만 조회/수정 가능하다).
- [x] 1.3 감사 로그 연계를 위해 기념일 CRUD 액션 타입을 정의한다 (검증: 생성/수정/삭제 시 로그 이벤트가 기록된다).

## 2. 서버 API 구현

- [x] 2.1 기념일 생성/수정/삭제/조회 핸들러를 구현한다 (검증: 통일 응답 포맷으로 CRUD가 동작한다).
- [x] 2.2 입력 검증(Zod)과 오류 코드 매핑(`VALIDATION_ERROR`, `ANNIVERSARY_NOT_FOUND`, `FORBIDDEN_OWNER`)을 적용한다 (검증: 잘못된 입력은 명시적 에러로 반환된다).
- [x] 2.3 범위 캘린더 조회 API에서 `exam_events + anniversaries` 병합 응답을 구현한다 (검증: 달력 응답에 두 도메인이 함께 포함된다).

## 3. 클라이언트 연동

- [x] 3.1 월별 캘린더 화면에서 서버 월 조회 API를 호출하도록 전환한다 (검증: 로컬 계산 없이 서버 응답으로 렌더링된다).
- [x] 3.2 기념일 추가/편집/삭제 UI 액션을 서버 CRUD와 연결한다 (검증: 각 액션 후 UI에서 데이터가 반영된다).
- [x] 3.3 서버 오류 코드별 사용자 안내 문구를 연결한다 (검증: 권한/검증 실패 시 이해 가능한 안내가 노출된다).

## 4. 통합 검증 및 문서화

- [x] 4.1 서버 테스트에 기념일 CRUD/소유권 RLS 시나리오를 추가한다 (검증: 권한 우회 요청이 차단된다).
- [x] 4.2 캘린더 통합 응답 테스트(일정 + 기념일 + 정렬)를 추가한다 (검증: 응답 정렬/형식이 일관된다).
- [x] 4.3 운영 문서에 기념일 API 오류 코드와 복구 절차를 업데이트한다 (검증: 문서만으로 장애 대응 절차를 수행할 수 있다).