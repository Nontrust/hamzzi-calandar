## 1. UI 토큰/레이아웃 기반 정리

- [x] 1.1 `apps/mobile-web/app/src/ui/theme.ts`에 공통 색상/간격/타이포/라운드 토큰을 정의한다. (검증: 토큰 파일 import 시 타입/빌드 오류가 없어야 한다)
- [x] 1.2 `apps/mobile-web/app/src/ui/appStyles.ts`를 생성해 로그인/메인 화면 공통 StyleSheet를 분리한다. (검증: `index.tsx` 인라인 스타일이 제거되고 앱이 동일하게 렌더링되어야 한다)

## 2. 화면 UI 쉘 개편

- [x] 2.1 로그인 화면에 로고/BI와 통일된 카드 레이아웃을 적용한다. (검증: 로그인 화면에서 브랜드 자산 2종이 정상 노출되어야 한다)
- [x] 2.2 메인 화면을 헤더/역할영역/요약카드/타임라인/빠른액션/권한섹션 구조로 재배치한다. (검증: 로그인 후 각 섹션이 구분된 카드 형태로 보여야 한다)

## 3. 실패 처리/회귀 검증

- [x] 3.1 캘린더/기념일/권한 요청 실패 시 마지막 정상 상태 유지 + 안내 문구 노출 동작을 점검하고 보완한다. (검증: 실패 후에도 기존 목록이 유지되고 화면이 중단되지 않아야 한다)
- [x] 3.2 모바일 웹 오프라인 실행으로 핵심 시나리오(로그인, 역할 전환, 기념일 CRUD, 권한 요청) 수동 검증 후 결과를 정리한다. (검증: `npm --workspace apps/mobile-web run web -- --offline` 실행 및 시나리오 통과)
