## 1. Workspace & Runtime Baseline

- [x] 1.1 `apps/mobile-web`, `services/functions`, `packages/{domain,ui,config}`, `infra/{migrations,scheduler}` 구조를 생성하고 워크스페이스 설정 파일을 정리한다 (검증: 루트에서 워크스페이스 인식 명령 실행 시 모든 패키지가 탐지된다).
- [x] 1.2 Expo(TypeScript) 앱 초기화 및 iOS/web 실행 스크립트를 설정한다 (검증: `expo start`에서 iOS/web 개발 서버가 정상 부팅된다).
- [x] 1.3 공통 환경 변수 로더(`packages/config`)를 만들고 클라이언트/서버에서 분리 로딩 규칙을 정의한다 (검증: 필수 env 누락 시 명시적 에러가 발생한다).

## 2. Supabase Foundation & Security

- [x] 2.1 Supabase 프로젝트 연결 및 기본 스키마 migration 틀을 만든다 (검증: migration 적용 후 빈 기본 스키마가 생성된다).
- [x] 2.2 `profiles`, `study_sessions`, `todo_items`, `exam_events`, `interview_sessions` 테이블을 생성한다 (검증: 각 테이블 생성 및 필수 컬럼 존재를 SQL로 확인한다).
- [x] 2.3 사용자 A/B 역할 기반 RLS 정책을 작성해 관리자 기능 접근을 제한한다 (검증: 사용자 B 토큰으로 관리자 데이터 쓰기 시도 시 거부된다).
- [x] 2.4 Supabase Storage 버킷(추억 사진)과 접근 정책을 구성한다 (검증: 허용된 역할만 업로드/조회 가능하다).

## 3. Server Functions for Integrations

- [x] 3.1 시험 일정 수집 함수(소스 호출 -> 표준 스키마 매핑 -> dedup key 계산)를 구현한다 (검증: 샘플 응답 입력 시 표준 이벤트 레코드가 생성된다).
- [x] 3.2 캘린더 동기화 함수(업서트 + 상태 전이)를 구현한다 (검증: 성공 시 `synced`, 실패 시 `failed`, 재시도 시 `pending` 전환이 기록된다).
- [x] 3.3 AI 모의면접 함수(Responses API 호출, 리포트 필수 필드 검증 후 저장)를 구현한다 (검증: 종료 요청 시 리포트 필드가 검증된다).
- [x] 3.4 스케줄러(cron)로 일정 수집/동기화 주기 실행을 등록한다 (검증: 수동 트리거와 예약 실행 모두에서 함수가 호출된다).

## 4. Client App Core Flows

- [x] 4.1 앱 진입 시 사용자 A/B 선택 및 활성 사용자 복원 플로우를 구현한다 (검증: 앱 재실행 시 마지막 활성 사용자가 복원된다).
- [x] 4.2 역할 기반 네비게이션 가드와 화면 접근 제어를 적용한다 (검증: 사용자 B에서 관리자 전용 화면 진입 시 차단된다).
- [x] 4.3 권한 요청(캘린더/사진/생체인증)을 기능 진입 시점 지연 요청 방식으로 구현한다 (검증: 앱 시작 시 권한 팝업이 뜨지 않고 기능 진입 시에만 요청된다).

## 5. Validation & Release Readiness

- [x] 5.1 상태 전이/권한/중복 제거에 대한 핵심 도메인 테스트를 추가한다 (검증: 테스트 스위트에서 관련 케이스가 모두 통과한다).
- [x] 5.2 iOS/web 수동 QA 시나리오(역할 전환, 세션 기록, 일정 동기화 실패 복구, AI 리포트 저장)를 수행한다 (검증: 체크리스트 기준 주요 경로 PASS 기록).
- [x] 5.3 운영 초기값과 롤백 절차(마이그레이션 되돌리기, 함수 비활성화)를 문서화한다 (검증: 문서만으로 복구 절차를 재현 가능하다).
