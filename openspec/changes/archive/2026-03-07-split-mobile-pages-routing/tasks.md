## 1. 라우트 구조 분리

- [x] 1.1 `app/(auth)`와 `app/(app)` 라우트 그룹 및 각 `_layout.tsx`를 생성한다. (검증: Expo Router가 경로를 정상 인식하고 타입 오류가 없어야 한다)
- [x] 1.2 로그인 화면을 `app/(auth)/login.tsx`로 분리하고 기존 로그인 UI/동작을 이관한다. (검증: 로그인 페이지 단독 진입 시 기존 로그인 동작이 동일해야 한다)
- [x] 1.3 홈 화면을 `app/(app)/index.tsx`로 분리하고 기존 홈 UI/동작을 이관한다. (검증: 로그인 후 홈 화면에서 기존 카드/액션/권한 UI가 동일하게 보여야 한다)

## 2. 인증 가드 및 라우팅 전환

- [x] 2.1 루트 또는 그룹 레이아웃에 인증 상태 기반 리다이렉트 로직을 구현한다. (검증: 미인증 상태에서 홈 접근 시 로그인으로 이동해야 한다)
- [x] 2.2 로그인 성공 시 홈으로, 로그아웃 시 로그인으로 즉시 전환하도록 연결한다. (검증: 로그인/로그아웃 직후 라우트가 기대 경로로 바뀌어야 한다)
- [x] 2.3 인증 초기화 로딩 상태(`isBootstrapping`)를 도입해 초기 화면 깜빡임을 방지한다. (검증: 앱 시작 시 보호 화면이 잘못 노출되지 않아야 한다)

## 3. 회귀 방지 및 검증

- [x] 3.1 페이지 분리 후 공통 UI 토큰/브랜딩 적용 일관성을 점검하고 누락 스타일을 보완한다. (검증: 로그인/홈 전환 시 색상/타이포/브랜딩 요소가 일관되어야 한다)
- [x] 3.2 핵심 시나리오(로그인, 역할 전환, 기념일 CRUD, 권한 요청, 오류 메시지)를 수동 검증한다. (검증: 기존 기능 회귀 없이 동작하고 실패 시 안내 문구가 유지되어야 한다)
- [x] 3.3 `npm --workspace apps/mobile-web run typecheck`를 실행해 타입 안정성을 확인한다. (검증: typecheck가 오류 없이 통과해야 한다)
